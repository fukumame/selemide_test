// group 'selenide_test'
// versionをつけることで、jarファイルの後ろに-1.0がつく
version '1.0'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile 'com.codeborne:selenide:2.25'
}
